/**
 * These query keys track cache entries for the remote data queries in react-query.
 */
const QUERY_KEYS = {
  /**
   * Tracks cached data.
   */

  GET_PROPERTIES: 'GET_PROPERTIES',
  GET_PROPERTIES_DETAILS: 'GET_PROPERTIES_DETAILS',
};

export default QUERY_KEYS;
